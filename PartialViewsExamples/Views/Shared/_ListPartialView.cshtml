@model ListModel

<div class="list-container">
    <h3>@Model.Title</h3>
    <ul class="list">
        @foreach (string item in Model.Tags)
        {
            <li>@item</li>
        }
    </ul>
</div>
